package app.actions;


import app.CommandHandler;

public class XX implements CommandHandler {
    @Override
    public void handle(String command) throws Exception {
        if (command.startsWith("")) {
            //do sth
        }
    }
}
